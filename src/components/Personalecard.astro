---


//const { href, title, body } = Astro.props;
---
<astro-card>
	<li	 class="link-card">
		<div class="img-container">
			<img class="image" src="/billede.png" alt="">
			<p class="body">
				Lorem ipsum dolor sit amet, consectetur adipiscing elit. Phasellus in lobortis nunc, id posuere mauris. Sed vel risus vitae ante tincidunt cursus eu et eros. Cras laoreet nisi at est semper posuere.
			</p>
			<div class="overlay"></div>
		</div>
		<div class="text-box">
			<h2>Navn</h2>
			<p>Titel</p>
		</div>
	</li>
</astro-card>	
<style>
	.link-card {
		position: relative;
		width: 300px;
		height: 395px;
		list-style: none;
		overflow: hidden;
		border-radius: 12px;
		box-shadow: 1px 4px 4px 0px rgba(0, 0, 0, 0.25);
		cursor: pointer;
		margin: 10px;
		background-color: var(--bright-white);
		transition: all 100ms ease;
	}
	.link-card:hover {
		box-shadow:  0px 4px 4px 2px rgba(0, 0, 0, 0.75);

	}
	.img-container {
		position: absolute;
		height: 75%;
		width: 100%;
	}
	.image {
		position: absolute;
		background-color: var(--main-blue);
		height: 100%;
		width: 100%;
		z-index: 2;
		bottom: 0;
		transition: all 400ms ease;
	}
	.overlay {
		position: absolute;
		height: 20%;
		width: 100%;
		background: linear-gradient(180deg, rgba(255,255,255,0) 0%, rgba(255,255,255,1) 100%);
		z-index: 3;
		bottom: 0;
		transition: all 400ms ease;
	}
	.overlay.is-active-1, .image.is-active-1{
		bottom: 115px;
	}

	.body {
		top: 60%;
		position: absolute;
		font-size: 0.9rem;
		padding: 10px;
		font-weight: 400;
		color: var(--main-blue);
	}
	.text-box {
		position: absolute;
		height: 80px;
		width: 100%;
		bottom: 0;
		padding: 15px;

	}
	.text-box p {
		font-weight: 200;
		color: var(--main-blue);
	}
</style>

<script>
	//class bruges her, til at differencere hver instance af komponentet
	//så hver af dem kan have sin egen funktionalitet.
	//constructoren af classen skaber en instans af komponentet, med koden nedenfor
	//inkluderet
		class Personalecard extends HTMLElement {
			constructor(){
				super();
				//variabler 
				//'this' keywoarded refererer til den specifikke instans
				const card = this.querySelector('li');
				const img = this.querySelector('.image');
				const overlay = this.querySelector('.overlay');
				//tilføj eventlistener
				card.addEventListener('click', () => {
						//toggle class med animation
						img.classList.toggle('is-active-1')
        				card.classList.toggle('is-active-1')
        				overlay.classList.toggle('is-active-1')
						console.log("test")
				});
		}
		}
		customElements.define('astro-card', Personalecard);
</script>
